# Personal folder (local only)

Create a `personal/` folder in this repo for **your private files**.
It is listed in `.gitignore` and will **never be pushed** to GitHub.

**Examples:** draft README sections, recruiter notes, profile experiments.

```bash
mkdir -p personal
```

See the workspace hub: `workspace/docs/PERSONAL-FOLDERS.md`
